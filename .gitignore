pkg/
tmp/
# RDoc's output, at the root. Anchored: unanchored it also swallowed
# docs/assets/doc/, where the manual's figures are, and they went uncommitted
# without a word — the site built with every one of them missing.
/doc/
.DS_Store
Gemfile.lock

*.bak
*.~
*.log
*.gem
log/
logs/

bin/rsync_code
bin/linktree.rb
tags
.tags
.rubocop.yml
.rubocop_todo.yml
.solargraph.yml

CLAUDE.md

# git filter-branch leaves this behind; it is machinery, not source.
.git-rewrite/
